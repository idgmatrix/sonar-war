{
  "name": "sonar-war",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "wasm:build": "wasm-pack build dsp-core --target web --out-dir pkg --dev",
    "wasm:build:release": "wasm-pack build dsp-core --target web --out-dir pkg --release",
    "wasm:test": "cargo test -p dsp-core"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
